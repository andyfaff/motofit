//automated batch file angle aligner//assumes that the sample is roughly at the right height.//you can't just you through this script in a cycle.//this is because there is deliberately no setting of offsets// To set the sth and sz offsets after the script type://sics("setpos sth 0.5" )), if you're using that angle of incidencesetdatafolder root:killvariables/z root:offset, root:angler_linevariable/g root:angler_lineangler_line = 0// change depending on entries in angler panel// if you run this multiple times then you should// only run this line once!angler(angler_line)// height scan first, move to centre once finisheddynskan("sztop", 2.5, 0.1, 21, automatic=1)// now work out anglefpx("dummy_motor",0,1,mode="time",preset=20,savetype=1,automatic=2)// figure out offset angle// change second entry, that's the direct beam number// first entry would correspond to scratch.nx.hdfvariable/g offset = wottpy(NaN, 64275)rel("sth", offset)print "relative sth offset so far is: ", getpos("sth") - str2num(root:packages:platypus:SICS:angler_listwave[angler_line][1])killvariables/z root:offset, root:angler_line